1、如果一个sql特别慢，很复杂。可以拆开成几个sql，在代码进行拼装。可能提高效率
保险销售数据表，和销售人员统计指标。
销售某种保险的指标，用union all 和分开查是一样的
2、查询索引列不要用函数计算，在java层计算好。再在sql中执行。不使用索引
3、能用主键查询不要用其他索引去查，会触发回表。避免回表
4、使用最左前缀原则。联合索引的前几个字段，或者一个索引的前部分字符 xxx%
5、对in的子查询用inner join优化。尽量使用覆盖索引。使用索引,可以用limit
6、对查询的列创建索引
7、sql中用到什么字段就写什么字段
8、对于or,如果or的
or两侧是同一个字段，且有索引，也在用limit优化
9、join时驱动表在前
10、limit


explain执行计划
type:
1、range表示进行了索引范围查询
2、index表示扫描了全部索引树如：desc SELECT school_code from t_physical_data_37 where school_code !="3437007174"，用到覆盖索引


1、加索引
2、对like进行优化，添加全文索引
3、对in查询，使用join优化
4、对查询已知数量的数据，用limit优化